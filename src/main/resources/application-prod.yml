# Production Profile
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  h2:
    console:
      enabled: false

logging:
  level:
    root: WARN
    com.example.microservice: INFO
    org.hibernate.SQL: WARN

swagger:
  enabled: false
